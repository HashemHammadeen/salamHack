from fastapi import APIRouter

router = APIRouter()

# The core MCP functionality is driven by the top-level mcp_server.py
# This router acts as a placeholder to allow Piecemint to manage the plugin state
# and display it properly in the plugins page.
